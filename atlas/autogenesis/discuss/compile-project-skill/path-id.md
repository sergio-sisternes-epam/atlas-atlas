---
type: document
title: "Orbit — Atlas path_id for compile discipline"
created: 2026-08-27
work_id: 2026-08-27-compile-project-skill
status: settled
kva: alive
reality: current
description: "Settled B1: path_id compile. Operator 2026-08-27."
origin: user
sensitivity: internal
stage: discussion
relates_to:
  - path: autogenesis/discuss/compile-project-skill/placement.md
    kind: follows
  - path: work/2026-08-27-compile-project-skill.md
    kind: implements
  - path: autogenesis/discuss/compile-project-skill/hub.md
    kind: related
---

## Content

Placement is B. Atlas already says CLI `compile` is a tool inside paths, not a catalog skill. This orbit names the path agents must load.

### Options

**B1 — `path_id: compile`.** Peer of query / remember / work / landscape. Module `references/paths/compile.md`. Activation card `path: compile`. Owns focused-vs-close-out, related-files rule, and when *not* to treat a green lens as session-close.

**B2 — No new path_id.** Fold the discipline into `remember` (writes already end on compile green) and maybe `work` close. Smaller surface. Easy to miss on query-only or discuss-only sessions that still dirty pages.

**B3 — Different name.** e.g. `hygiene` or `close`. Avoids verb-clash with `atlas compile`. Costs a second name for the same gate.

### Tension

Remember already claims compile-green on write. A first-class `compile` path is for sessions that are not remember: discuss persist, implement edits, landscape, repair walks. If the only home is remember, agents who never enter remember never load the rule.

`path: compile` matching the CLI verb is consistent with `path: query` vs `atlas search` (names already diverge there). Aligning path and CLI on `compile` is the tighter map.

### Not this orbit

Related-files definition. Implement. Extra CLI flags.

## Answer (operator 2026-08-27)

**B1.** `path_id: compile`. Module `references/paths/compile.md`. B2 and B3 are alternative, not current. B3 name is unused.

## Batch

1. Settled B1.
2. Unused.
3. Moved to `enter-scope.md`.
